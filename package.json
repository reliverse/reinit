{
  "name": "@reliverse/reinit-cli",
  "version": "1.0.1",
  "description": "@reliverse/reinit is your buddy for bootstrapping the boring stuff — so you can jump straight into building the good stuff.",
  "type": "module",
  "scripts": {
    "dev": "bun src/main.ts --dev",
    "agg": "relidler tools --tool agg --input src/libs/reinit/reint-impl --out src/libs/reinit/reinit-main.ts --recursive --named --strip src/libs/reinit"
  },
  "dependencies": {
    "@reliverse/prompts": "^1.6.0",
    "c12": "^3.0.2",
    "fs-extra": "^11.3.0",
    "p-map": "^7.0.3",
    "pathe": "^2.0.3"
  },
  "devDependencies": {
    "@reliverse/cli-cfg": "^1.6.2",
    "@reliverse/relidler-cfg": "^1.1.3",
    "@types/fs-extra": "^11.0.4"
  }
}